Azure Key Vault

to place our secret information
and
encrypted information

e.g.
rather than putting info in appsettings
like connection string 
better practice is to keep there

In Azure Vault we can add
	secrets - Key Value pair
			directory path
or 	keys 	(encryption key)
		to store encrypted pwd
		in database

	private and public key

we can send data to azure in plain text
azure key will convert it to encrypted
value and then store in database

demo target - create vault
	create secret and keys
	access values thru C#

IP Address - 4 octets
	10.192.0.11

Azure Virtual Network - 
	can have a set of objects like
	vms or printer etc
	SubNet 	

range of address - 


Windows Web Server 	IIS

after installing IIS
	IIS manager 

c:\inetpub\wwwroot

first.html

browser

http://localhost/first.html
localhost is name of ip address 

	127.0.0.1
	index.html

deploy 
Linux			


rg-appgw-lab

VNet - vnet-appgw 

Subnets - 1   appgw-subnet
	  2   backend-subnet

VM1	Private 10.0.1.4
VM2	Private 10.0.1.5	





That error is straightforward:

Microsoft.Cdn is not registered for the subscription
means your Azure subscription hasn’t enabled the CDN resource provider yet.

✅ Fix (Portal – easiest for demo)
Go to Azure Portal
Search → Subscriptions
Select your subscription
Go to Resource providers
Search: Microsoft.Cdn
Click Register
Azure Portal
App Services
can be used to deploy 
various types of applications
	e.g. Web API, MVC, Angular etc

Let us create a new web api - azureAppAPI

Add API project to source control
	git will be initialized for this project

local git and remote git






Create a Web API using EF SQL Server (local)

add migration, update database
test web api on swagger
data should be added locally

we have to replace local SQL server 
	with azure database and SQL Server

Create azure SQL database
as a part of DB creation, server will
	also be created

  A new azure option is serverless SQL Server
	It is not like there is no server
	but it is sharing low cost
	azure maintained SQL Server
	we don't have to manage it

sksadmin   Sharad@123


Server=tcp:skssqlserver123.database.windows.net,1433;Initial Catalog=sksazuresqldb;Persist Security Info=False;User ID=sksadmin;Password=Sharad@123;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

	
bcos we created a new connection string in appsettings
  	do update-database again


App Service Plan 
Once created do not delete this

Web App

APIM

Azure API Management 

We have to create one App Service
Add API to it
APIM

gateway
limitation on APIs calls
e.g. I want to restrict continuous 
	request count

it provides applying web api key


Azure Functions
Why to use Function
to put functionality as a method 
on cloud to be cost as per consumption
these functions run based on 
any event like http trigger
		blob trigger
		timer trigger

we will use Visual Studio to create the 
	function and publish it

Create a new project - azure function


Will create azure function App 





http://requestfunctions?a=11&b=22











Add a Storage Account
Add a container
upload a file/image

attach a azure function 
the user/admin should get an email 

email sending is separate feature that requires queues
	and resend option on fail  

For sending email we will use another service - 
	Azure Logic App

Logic apps are workflows - we add multiple functionalities 
		as a flow (steps) 





{
  "To":"azuretestapr26@gmail.com",
  "Subject":"New Upload",
  "Body": "New BLOB added to the Storage"
}







